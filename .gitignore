node_modules/
dist/
coverage/
*.tsbuildinfo

# Local runtime state — never commit a real ledger or profile snapshot
.foster/
*.ledger.jsonl

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Agent scratch: worktrees and checkpoints land under .claude/, and only the
# commands are meant to be tracked. Salvaged from PR #10, whose docs were
# superseded but whose guard was not.
.claude/*
!.claude/commands/
