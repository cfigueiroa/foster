node_modules/
dist/
coverage/
*.tsbuildinfo

# Local runtime state — never commit a real ledger or profile snapshot
.foster/
*.ledger.jsonl

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Claude Code runtime state. The repository tracks .claude/commands/ and nothing
# else under here: worktrees, checkpoints, mailboxes and agent registries are
# per-machine, and this repository is public.
.claude/*
!.claude/commands/
